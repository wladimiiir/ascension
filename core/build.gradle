apply plugin: "java"

[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

sourceSets {
  main {
    java {
      srcDir 'src'
    }
    resources {
      srcDir 'assets'
    }
  }
}

eclipse.project {
    name = appName + "-core"
}
